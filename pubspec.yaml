name: tekeraheza_mobile
description: Tekeraheza LPG Gas Distribution — mobile app (Delivery & Management Portal).
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  font_awesome_flutter: ^10.6.0

  http: ^1.1.0
  dio: ^5.4.0
  connectivity_plus: ^5.0.2

  provider: ^6.1.1
  get_it: ^7.6.4
  go_router: ^14.2.0

  shared_preferences: ^2.2.2
  intl: ^0.19.0
  logger: ^2.0.2
  fl_chart: ^0.69.0
  image_picker: ^1.0.7
  url_launcher: ^6.2.5
  cached_network_image: ^3.3.1
  google_sign_in: ^6.2.2
  file_picker: ^8.1.4
  google_maps_flutter: ^2.12.3
  geolocator: ^14.0.2
  path_provider: ^2.1.5
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
